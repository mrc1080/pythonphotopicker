# Photo Picker

A Windows-friendly photo review tool for burst photos.

## What it does
- Groups photos taken close together (bursts)
- Helps you keep the best shots
- Moves duplicates/extras to an Extras folder
- Preview-only rotation (original files are never modified)

## Development setup
```powershell
python -m venv .venv
.\.venv\Scripts\activate
pip install -r requirements.txt
